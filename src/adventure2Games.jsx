import React,{useState}from'react';
export function ScaleGame({lesson,onSolved}){const[v,setV]=useState(lesson.start??1),[m,setM]=useState('Slide to resize every part together.');const check=()=>Math.abs(v-lesson.target)<.01?(setM(lesson.explanation),onSolved()):setM(`You are at ${v}×. Aim for ${lesson.target}×.`);return <><div className="scaleStage"><div className="scaleRobot" style={{transform:`scale(${Math.max(.5,Math.min(1.5,v/1.5))})`}}>🤖</div><b>{v}× size</b><input type="range" min="0.5" max="3" step="0.5" value={v} onChange={e=>setV(+e.target.value)}/></div><button className="primary" onClick={check}>Check the scale</button><p className="feedback">{m}</p></>}
export function EquivalenceGame({lesson,onSolved}){const[s,setS]=useState([]),[m,setM]=useState('Select every card that means the target amount.');const check=()=>{const ok=s.length===lesson.correct.length&&lesson.correct.every(x=>s.includes(x));ok?(setM(lesson.explanation),onSolved()):setM('Some representations do not describe the same amount yet.')};return <><div className="equivTarget">Target <b>{lesson.target}</b></div><div className="matchBoard">{lesson.choices.map(x=><button key={x} className={s.includes(x)?'matchTile selected':'matchTile'} onClick={()=>setS(v=>v.includes(x)?v.filter(y=>y!==x):[...v,x])}>{x}</button>)}</div><button className="primary" onClick={check}>Connect the representations</button><p className="feedback">{m}</p></>}
export function ScaleMapGame({lesson,onSolved}){const[v,setV]=useState(0),[m,setM]=useState('Slide until the map and real distance agree.');const check=()=>v===lesson.target?(setM(lesson.explanation),onSolved()):setM(`${lesson.mapUnits} map blocks at ${lesson.factor} each needs a different real distance.`);return <><div className="mapScaleStage"><div>🗺️ <b>{lesson.mapUnits} blocks</b></div><div className="scaleArrow">→</div><div>🛣️ <b>{v} miles</b></div></div><input className="aimSlider" type="range" min="0" max="15" value={v} onChange={e=>setV(+e.target.value)}/><button className="primary" onClick={check}>Check distance</button><p className="feedback">{m}</p></>}
export function RaceGame({lesson,onSolved}){const[t,setT]=useState(1),[m,setM]=useState('Change the time and watch the pace change.');const rate=(lesson.distance/t).toFixed(1);const check=()=>t===lesson.timeTarget?(setM(lesson.explanation),onSolved()):setM(`That is ${rate} spaces each second. Keep tuning.`);return <><div className="raceTrack"><span style={{left:`${Math.min(90,10+Number(rate)*18)}%`}}>🏎️</span><i/></div><div className="raceControls"><b>{lesson.distance} spaces</b><input type="range" min="1" max="8" value={t} onChange={e=>setT(+e.target.value)}/><b>{t} sec → {rate}/sec</b></div><button className="primary" onClick={check}>Run the race</button><p className="feedback">{m}</p></>}
export function MachineGame({lesson,onSolved}){const[v,setV]=useState(0),[m,setM]=useState('Each card uses one secret rule. Compare what changed from IN to OUT.'),[tested,setTested]=useState(false);const delta=lesson.pairs?.length&&lesson.pairs.every(([a,b])=>b-a===lesson.pairs[0][1]-lesson.pairs[0][0])?lesson.pairs[0][1]-lesson.pairs[0][0]:null;const check=()=>{setTested(true);v===lesson.target?(setM(lesson.explanation),onSolved()):setM(delta!==null?`Try the same change on ${lesson.input}. In every example, the machine ${delta>=0?'adds':'takes away'} ${Math.abs(delta)}.`:'Compare all three example cards. The machine must do exactly the same thing each time.')};return <><div className="machineLesson"><div className="machineCoach"><span>🤖</span><div><small>MYSTERY MACHINE</small><b>What does my machine do every time?</b><p>Follow each row from left to right: number goes in → machine changes it → new number comes out.</p></div></div><div className="machineExamples">{lesson.pairs.map(([a,b],i)=><div className="machineExample" key={a}><span className="exampleTag">Example {i+1}</span><div className="machineNumber"><small>GOES IN</small><b>{a}</b></div><span className="flowArrow">→</span><div className="machineBox"><span>⚙️</span><small>SECRET RULE</small></div><span className="flowArrow">→</span><div className="machineNumber output"><small>COMES OUT</small><b>{b}</b></div></div>)}</div><div className="machineQuestion"><small>NOW YOU TRY</small><h3>{lesson.reverse?'What must go in?':'What comes out?'}</h3><div className="machineExample activeMachine"><div className="machineNumber"><small>{lesson.reverse?'COMES OUT':'GOES IN'}</small><b>{lesson.reverse?'11':lesson.input}</b></div><span className="flowArrow">→</span><div className="machineBox mysteryGear"><span>⚙️</span><small>SAME SECRET RULE</small></div><span className="flowArrow">→</span><div className="machineNumber answerNumber"><small>{lesson.reverse?'GOES IN':'YOUR OUTPUT'}</small><b>{v}</b></div></div><div className="machineStepper"><button onClick={()=>setV(x=>Math.max(0,x-1))}>−</button><div><small>YOUR ANSWER</small><b>{v}</b></div><button onClick={()=>setV(x=>Math.min(15,x+1))}>+</button></div>{delta!==null&&<div className="machineClue"><b>Look for the change:</b> {lesson.pairs[0][0]} becomes {lesson.pairs[0][1]}. Does that same change work for the other examples?</div>}</div></div><button className="primary" onClick={check}>Run {lesson.reverse?'my input':'my answer'} through the machine</button><p className={`feedback ${tested?'':'machinePrompt'}`}>{m}</p></>}
const apply=(v,op)=>op==='mul2'?v*2:op==='add2'?v+2:op==='add1'?v+1:op==='sub1'?v-1:op==='sub2'?v-2:v;
export function RuleBlocksGame({lesson,onSolved}){const[seq,setSeq]=useState([]),[m,setM]=useState('Tap operation blocks in the order you want the machine to use them.');const result=seq.reduce((v,op)=>apply(v,op),lesson.start);const check=()=>result===lesson.target&&seq.join(',')===lesson.solution.join(',')?(setM(lesson.explanation),onSolved()):setM(`Your rule turns ${lesson.start} into ${result}. Try rearranging it.`);return <><div className="ruleFlow"><span>{lesson.start}</span>{seq.map((op,i)=><span key={i}>{lesson.blocks.find(b=>b.op===op)?.label}</span>)}<span>= {result}</span></div><div className="hubTiles">{lesson.blocks.map(b=><button key={b.op} onClick={()=>setSeq(v=>[...v,b.op])}>{b.label}</button>)}<button onClick={()=>setSeq([])}>Reset</button></div><button className="primary" onClick={check}>Run my rule</button><p className="feedback">{m}</p></>}
export function SlopeGame({lesson,onSolved}){const[rise,setRise]=useState(1),[run,setRun]=useState(1),[m,setM]=useState('Build the path by changing how far it rises and runs.');const angle=Math.atan(rise/run)*180/Math.PI;const check=()=>rise===lesson.riseTarget&&run===lesson.runTarget?(setM(lesson.explanation),onSolved()):setM(`Right now your path rises ${rise} for every ${run} across. Keep shaping it.`);return <><div className="slopeBuilder"><div className="slopeVisual"><div className="slopeGround"/><div className="slopePath" style={{width:`${Math.min(82,28+run*7)}%`,transform:`rotate(${-angle}deg)`}}><span className="slopeHiker">🥾</span></div><div className="slopeRiseGuide"><span>↑</span><b>rise {rise}</b></div><div className="slopeRunGuide"><span>→</span><b>run {run}</b></div></div><div className="slopeControls"><label><span><b>Rise</b><strong>{rise}</strong></span><input type="range" min="1" max="6" value={rise} onChange={e=>setRise(+e.target.value)}/></label><label><span><b>Run</b><strong>{run}</strong></span><input type="range" min="1" max="8" value={run} onChange={e=>setRun(+e.target.value)}/></label></div></div><button className="primary" onClick={check}>Check my path</button><p className="feedback">{m}</p></>}
export function DealGame({lesson,onSolved}){const[m,setM]=useState('Compare what one item costs, not just the package price.');return <><div className="dealCards">{lesson.deals.map((d,i)=><button key={i} onClick={()=>i===lesson.correctIndex?(setM(lesson.explanation),onSolved()):setM(`${d.label} costs ${d.unit} coins per item. Compare that with the other deal.`)}><span>🛒</span><b>{d.label}</b><small>{d.unit} coins each</small></button>)}</div><p className="feedback">{m}</p></>}
export function MissionGame({lesson,onSolved}){const[stage,setStage]=useState(0),[v,setV]=useState(0),[done,setDone]=useState([]),[m,setM]=useState('Bring all three systems online to launch the mission.');const s=lesson.stages[stage],icons=['🗺️','🏎️','⚙️'];const stepDown=()=>setV(x=>Math.max(0,x-1)),stepUp=()=>setV(x=>Math.min(15,x+1));const check=()=>{if(v!==s.target){setM('That system is not ready yet. Check the relationship and try again.');return}const nextDone=[...done,stage];setDone(nextDone);if(stage===lesson.stages.length-1){setM('All three systems are online — mission launched!');onSolved()}else{setStage(x=>x+1);setV(0);setM(`${s.label} system online! Bring the next control online.`)}};return <><div className="missionChecklist">{lesson.stages.map((x,i)=><div key={x.label} className={`${i===stage?'activeMissionStep ':''}${done.includes(i)?'doneMissionStep':''}`}><span>{done.includes(i)?'✅':icons[i]||'🔧'}</span><div><small>CONTROL {i+1}</small><b>{x.label}</b></div></div>)}</div><div className="missionConsole"><div className="missionVisual"><span>{icons[stage]||'🔧'}</span><div><small>CONTROL {stage+1} OF {lesson.stages.length}</small><h3>{s.label}</h3></div></div><div className="missionProblem"><p>{s.question}</p><div className="missionAnswerControl"><button onClick={stepDown}>−</button><div><small>YOUR SETTING</small><b>{v}</b></div><button onClick={stepUp}>+</button></div></div></div><button className="primary" onClick={check}>{stage===lesson.stages.length-1?'Launch mission':'Bring system online'}</button><p className="feedback">{m}</p></>}
